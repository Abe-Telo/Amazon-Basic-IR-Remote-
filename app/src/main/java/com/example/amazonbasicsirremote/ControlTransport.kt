package com.example.amazonbasicsirremote

// Kotlin-facing transport metadata placeholder. The Android module currently
// compiles Java sources only, so the Java ControlTransport enum is the runtime
// source of truth until the Kotlin Gradle plugin is enabled for app sources.
